# UEproject

Private Unreal Engine Project which was supposed to be a RPG.
Uses XML for NPC Data and can utilize multiple search functions for pathfinding in the waynet (Graph represented by Waypoints for NPC paths).
